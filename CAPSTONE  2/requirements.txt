Django==5.0.12
python-dotenv==1.0.1
Pillow==12.0.0
django-vite==3.1.0
django-htmx==1.27.0
gunicorn==23.0.0
whitenoise==6.8.2
dj-database-url==2.3.0
psycopg2-binary==2.9.11
# Email over HTTPS (Brevo) because Render's free tier blocks SMTP ports.
# The BREVO_API_KEY env var activates it in settings.py, which imports
# this package - removing it takes the whole site down.
django-anymail==13.1
# Media storage (profile pictures, prescription attachments). Render's disk
# is ephemeral and doesn't serve /media/ in production at all, so uploads
# go to Cloudinary instead when CLOUDINARY_URL is set - see settings.py.
django-cloudinary-storage==0.3.0
cloudinary==1.41.0
